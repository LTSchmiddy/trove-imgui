#include "../base/fragment.h"
#include "../core.h"

namespace UI {
class VideoPlayer : public Fragment {
    // bool onEvent(SDL_Event* event) override;
    // void onBackground() override;
    void onDraw() override;
};
}